import React from 'react';
import Table from '../../../UI/Table/Table';

const Primes = () => {
  return (
    <div>
      <Table
        intersect="Nomenclatura"
        headers={['Header 1']}
        columns={['Column 1']}
        data={[[]]}
      />
    </div>
  );
};

export default Primes;
