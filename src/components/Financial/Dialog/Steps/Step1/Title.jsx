import React from 'react';
import Typography from '@material-ui/core/Typography';

const Title = () => {
  return <Typography>Paso 1. Define la estructura de pago</Typography>;
};

export default Title;
